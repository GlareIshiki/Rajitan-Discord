# Rajitan（らじたん）Discord Bot 実行手順書

## 📋 事前準備

### 0. Pythonバージョン要件

**重要**: このBotはPython 3.13では動作しません。
- **推奨**: Python 3.11.x または 3.12.x
- **非対応**: Python 3.13.x（audioopモジュールが削除されたため）

#### Pythonバージョン確認
```bash
python --version
```

#### Python 3.11のインストール（必要な場合）
1. [Python 3.11.9](https://www.python.org/downloads/release/python-3119/)をダウンロード
2. インストール時に「Add Python 3.11 to PATH」にチェック
3. インストール後、新しいターミナルで確認：
   ```bash
    py -3.11 --version
   ```

### 1. 必要なアカウント・APIキーの取得

#### Discord Bot Token
1. [Discord Developer Portal](https://discord.com/developers/applications)にアクセス
2. 「New Application」をクリックしてアプリケーションを作成
3. 左メニューから「Bot」を選択
4. 「Add Bot」をクリック
5. 「Token」セクションから「Copy」でBotトークンを取得
6. **重要**: トークンは他人に見せないよう注意してください

#### OpenAI API Key
1. [OpenAI API](https://platform.openai.com/)にサインアップ
2. 「API Keys」ページで新しいAPIキーを作成
3. APIキーをコピーして保存

#### Redis（オプション）
- ローカル開発の場合は不要（メモリキャッシュが使用されます）
- 本番環境では[Redis](https://redis.io/)の設置を推奨

### 2. Botの権限設定

Discord Developer Portalで以下の権限を設定：

**Bot Permissions:**
- Send Messages
- Use Slash Commands
- Read Message History
- Add Reactions
- Embed Links
- Attach Files

**OAuth2 Scopes:**
- `bot`
- `applications.commands`

## 🚀 インストール手順

### 1. プロジェクトのセットアップ

```bash
# 1. プロジェクトディレクトリに移動
cd Rajitan-Discord

# 2. 既存の仮想環境を削除（存在する場合）
# Windows:
Remove-Item -Recurse -Force venv
# macOS/Linux:
rm -rf venv

# 3. Python 3.11で仮想環境の作成（推奨）
py -3.11 -m venv venv

# 4. 仮想環境の有効化
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 5. 依存関係のインストール
pip install -r requirements.txt
```

### 2. 環境設定

```bash
# 1. 環境設定ファイルの作成
cp .env.example .env

# 2. .envファイルを編集（重要）
# 必要最小限の設定:
```

**.envファイルの編集例:**
```env
# 必須項目（必ず設定してください）
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# オプション項目（基本設定のまま使用可能）
DEBUG=True
LOG_LEVEL=INFO
CONVERSATION_TIMEOUT=3600
SUMMARY_INTERVAL=1800
QUIZ_INTERVAL=3600

# Redis設定（ローカル開発では不要）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
```

### 3. データベース初期化

```bash
# SQLiteデータベースは自動で作成されるため特別な設定は不要
# ログディレクトリの作成
mkdir -p logs
```

## 🎮 実行手順

### 1. Bot起動

```bash
# 仮想環境が有効化されていることを確認
# プロンプトに (venv) が表示されているはず

# メインアプリケーションの実行
python -m rajitan.main
```

**正常起動時のログ例:**
```
2025-01-05 10:00:00 - rajitan.main - INFO - Initializing Rajitan application...
2025-01-05 10:00:01 - rajitan.sqlite_client - INFO - SQLite database initialized successfully
2025-01-05 10:00:01 - rajitan.redis_client - INFO - Redis client initialized successfully
2025-01-05 10:00:02 - rajitan.discord_client - INFO - らじたん has connected to Discord!
2025-01-05 10:00:02 - rajitan.discord_client - INFO - Bot is in 1 guilds
```

### 2. DiscordサーバーへのBotの招待

1. Discord Developer Portalの「OAuth2」→「URL Generator」
2. **Scopes**: `bot`, `applications.commands`を選択
3. **Bot Permissions**: 必要な権限を選択
4. 生成されたURLでBotをサーバーに招待

### 3. 初期セットアップ（Discord内）

```
# 1. キャラクター設定
/setup name:らじたん personality:default

# 2. 動作確認
/status

# 3. テスト会話
/chat message:こんにちは！
```

## 🔍 動作確認手順

### 1. 基本機能テスト

```bash
# Discord上で以下を順番に実行:

# 1. ヘルプ表示
/help

# 2. ボット状態確認
/status

# 3. キャラクター設定
/setup name:テストらじたん personality:cheerful

# 4. 直接会話
/chat message:自己紹介してください

# 5. メンション会話
@らじたん 今日の天気はどう？
```

### 2. 機能テスト

```bash
# 1. 会話を複数人で10-15回行う
# （複数のユーザーでテスト会話を実施）

# 2. 要約機能テスト
/summary

# 3. クイズ機能テスト
/quiz

# 4. クイズ回答テスト
A
# または B, C, D で回答
```

## 🛠️ トラブルシューティング

### よくあるエラーと対処法

#### 1. Python 3.13関連エラー
```
ModuleNotFoundError: No module named 'audioop'
```
**対処法:**
- Python 3.11または3.12を使用してください
- 仮想環境を削除して、Python 3.11で再作成してください

#### 2. Bot Token エラー
```
discord.errors.LoginFailure: Improper token has been passed.
```
**対処法:**
- `.env`ファイルの`DISCORD_BOT_TOKEN`が正しく設定されているか確認
- トークンにスペースや改行が含まれていないか確認

#### 3. OpenAI API エラー
```
openai.error.AuthenticationError: Incorrect API key provided
```
**対処法:**
- `.env`ファイルの`OPENAI_API_KEY`が正しく設定されているか確認
- OpenAIアカウントの利用制限を確認

#### 4. 権限エラー
```
discord.errors.Forbidden: 403 Forbidden (error code: 50013)
```
**対処法:**
- Botがメッセージを送信権限を持っているか確認
- スラッシュコマンドが正しく同期されているか確認

#### 5. Redis接続エラー
```
redis.exceptions.ConnectionError
```
**対処法:**
- Redisが不要な場合は無視（メモリキャッシュにフォールバック）
- Redisを使用したい場合はRedisサーバーを起動

### ログの確認方法

```bash
# リアルタイムログ確認
tail -f logs/rajitan.log

# エラーログのみ確認
grep "ERROR" logs/rajitan.log

# 特定機能のログ確認
grep "character_manager" logs/rajitan.log
```

## 🔄 運用手順

### 日常運用

1. **Bot起動**
   ```bash
   # 仮想環境を有効化
   venv\Scripts\activate  # Windows
   source venv/bin/activate  # macOS/Linux
   
   # Bot起動
   python -m rajitan.main
   ```

2. **ログ監視**
   ```bash
   tail -f logs/rajitan.log
   ```

3. **定期的な確認事項**
   - OpenAI API使用量
   - ディスク容量（ログファイルサイズ）
   - Botの応答速度

### メンテナンス

1. **ログローテーション**
   ```bash
   # 古いログファイルのアーカイブ
   mv logs/rajitan.log logs/rajitan_$(date +%Y%m%d).log
   ```

2. **データベース最適化**
   ```bash
   # SQLiteデータベースの最適化
   sqlite3 rajitan.db "VACUUM;"
   ```

### バックアップ

```bash
# 重要なファイルのバックアップ
cp rajitan.db backup/rajitan_$(date +%Y%m%d).db
cp .env backup/.env_$(date +%Y%m%d)
```

## 📞 サポート・問い合わせ

- **ログファイル**: `logs/rajitan.log`を確認
- **設定確認**: `/status`コマンドでBot状態を確認
- **問題報告**: GitHubのIssuesページで報告

## 🎯 成功指標

正常に動作している場合の確認ポイント：

- [ ] Python 3.11または3.12が使用されている
- [ ] Botがオンライン状態でDiscordに接続
- [ ] `/status`コマンドが正常に応答
- [ ] メンション時に自然な返答
- [ ] `/summary`で会話要約が生成される
- [ ] `/quiz`でクイズが開始される
- [ ] ログにERRORレベルのメッセージが出ていない

これらすべてが✓の場合、らじたんは正常に動作しています！